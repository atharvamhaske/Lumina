import { NextRequest, NextResponse } from "next/server";

export async function signUphandler(req: NextRequest, res: NextResponse) {
    try {
        if(!email)
    } catch (error) {
        
    }

} 